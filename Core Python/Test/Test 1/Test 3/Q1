#1. Write a program to print first n prime numbers
n = int(input("Enter how many prime numbers to print: "))

count = 0  # Counts how many primes have been found
num = 2    # Number to check for primality

while count < n:
    # Check if num is prime
    is_prime = True
    for i in range(2, int(num**0.5) + 1):
        if num % i == 0:
            is_prime = False
            break
    if is_prime:
        print(num, end=" ")
        count += 1
    num += 1
